
20190119  第1回memo

・MS社JavaベースのJ++ -> ライセンス問題 -> C#

・CUI -> 主にサーバで使用される。ジョブなどをコマンドで管理するため。

-----
・using：外部ライブラリを使用する。
・namespace：classを目的ごとにまとめる。
・class：情報そのものを呼ぶ。
-----

・classごとのまとまりより外の領域をstatic areaと呼ぶ。

・プログラムをスタートさせる時、各class情報を把握して関係を整理する。
　-> このとき参照先がないとエラー。
　-> その後Main関数から順番に実行される。

・globalとlocal
  -> メンバー(=フィールド)は、classの中で、共通で使用する変数を指す。

・ string aaa = null;   ->  名札を作っただけ。箱は用意していない。
   string bbb = "";     ->  名札を作って、箱に貼った。ただ、中には何も無い状態。



